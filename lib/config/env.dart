// Este archivo lo uso para definir configuraciones globales del proyecto.
// Aquí podría poner la URL de la API del backend o parámetros generales.
class Env {
  static const String apiUrl = "http://localhost:8080/api";
  // En este caso, esta sería la dirección donde se conecta con mi backend en Spring Boot.
}
